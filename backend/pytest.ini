[pytest]
testpaths = tests
python_files = test_*.py
# 默认临时目录在 %LOCALAPPDATA%\Temp 下，本机该目录对 os.scandir 拒绝访问，
# 会让所有用到 tmp_path 的用例直接 error。固定到仓库内的忽略目录。
addopts = -q --basetemp=.pytest-tmp
filterwarnings =
    ignore::DeprecationWarning
